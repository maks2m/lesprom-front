
/*
export default {
    sortedTPOnOrderAndOperationCode(technologicalProcesses, orderId) {
        const TP = technologicalProcesses.filter(t => t.order.id === orderId);
        return TP.sort((firstTP, secondTp) => firstTP.operationCode - secondTp.operationCode);
    },
    sortedTPOnOperationCode(technologicalProcesses) {
        return technologicalProcesses.sort((firstTP, secondTp) => firstTP.operationCode - secondTp.operationCode);
    },
    test(technologicalProcesses) {


        return false;
    },
}
*/

